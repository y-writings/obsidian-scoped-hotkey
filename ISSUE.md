# コンテキストインスペクター UI/UX 課題

## レビュー前提

- 対象機能: `Inspect next click context` を実行し、次にクリックした箇所のコンテキストをモーダルへ表示する機能
- 想定ユーザー: IDEなどでキーバインドを日常的に設定し、`when` 条件やスコープの概念を理解しているWebエンジニア
- ユーザーの目的: DOMを調査することではなく、「この場所で有効になるショートカット」を正しく、素早く設定するための条件を得ること
- 評価対象: 添付画面、Obsidian上の実機表示、`src/main.ts`、`src/context.ts`、`src/context-modal.ts`、`styles.css`

## 結論

機能の必要性はある。しかし、現在の「コマンド実行後の次回クリックを捕捉し、内部値を一覧表示する」という実現方法は、ショートカットが実際に評価されるコンテキストと一致しない可能性があり、検査操作そのものがObsidianを操作してしまう。

最大の問題は外観ではなく、検査結果の正しさと操作の安全性をユーザーが信頼できないことである。P0の3件を解消するまでは、この画面から得た条件をキー割り当て機能へ接続すべきではない。

| ID | 優先度 | 課題 |
| --- | --- | --- |
| UX-001 | P0 | クリック位置とホットキー実行時コンテキストが一致しない |
| UX-002 | P0 | 検査用クリックが元のUI操作も発火させる |
| UX-003 | P0 | 検査待機状態が不可視かつ解除不能である |
| UX-004 | P1 | 検査対象外のクリックを有効な結果として扱う |
| UX-005 | P1 | 「候補」と「完成した条件」の区別がなく、過剰に限定された条件を誘発する |
| UX-006 | P1 | 結果画面に次の行動がなく、設定作業が完結しない |
| UX-007 | P1 | 主目的より内部診断情報が強く、判断に必要な情報階層になっていない |

## UX-001: クリック位置とホットキー実行時コンテキストが一致しない

**優先度:** P0

### 課題

ショートカットはキー入力時のアクティブビューとフォーカスを基準に実行される。一方、現在の推奨条件は「次にクリックされた要素を含むLeaf」を最優先に生成している。マウスのヒットテスト結果を、キーボード操作時の実行コンテキストとして代用している状態である。

さらに、クリックリスナーはキャプチャフェーズで動作するため、`document.activeElement` はクリックによるフォーカス移動より前の値である可能性が高い。表示される `Focused element` は、ユーザーが選んだ対象ではなく、直前にフォーカスされていた別要素になり得る。

### 根拠

- `src/main.ts:89` は `click` をキャプチャフェーズで監視している。
- `src/main.ts:80` はそのイベント処理中に即座にコンテキストを収集している。
- `src/context.ts:79` はその時点の `activeElement` を取得している。
- `src/context.ts:84` は `clickedLeaf ?? focusedLeaf ?? sameDocumentActiveLeaf` の順でLeafを選ぶため、クリック位置がキーボードフォーカスより優先される。
- IDE系のキーバインドでユーザーが期待するのは「キーを押した瞬間にどのエディタ、ビュー、入力欄がアクティブか」であり、「直前にどこをクリックしたか」ではない。
- W3CのKeyboardガイダンスも、ポインター操作に限定せず、同等の機能をキーボードインターフェースから実行可能にすることを求めている。

### 通常利用での影響

メインエディタで編集しているユーザーが右サイドバーを検査した場合、クリックしたLeaf、クリック前の入力フォーカス、アクティブLeafが異なる組み合わせになり得る。この結果をそのまま条件化すると、設定したホットキーが意図した場所で反応しない、または別の場所で反応する。

これは特殊なコーナーケースではない。複数ペイン、サイドバー、検索入力、コミュニティプラグインのビューを併用する通常のObsidian操作で発生する。

### 改善方針

主導線を「次のクリックを検査」から「現在のホットキー実行コンテキストを検査」へ変更する。将来のホットキー判定とインスペクターで、同じコンテキスト解決処理を使用する。

ポインターによる対象選択は補助機能として残してよいが、その場合も「クリックしたDOM」と「ホットキー判定に使うアクティブ/フォーカスコンテキスト」を混同しない。両者が異なる場合は、その差を明示する。

### 受け入れ条件

- [ ] 現在のアクティブビューをキーボード操作だけで検査できる。
- [ ] インスペクターの推奨条件と、将来のホットキーディスパッチャーが同じコンテキスト解決処理を使用する。
- [ ] クリック対象、アクティブLeaf、フォーカス要素が異なる場合、どれを条件判定に使うかが画面上で明示される。
- [ ] コマンドパレット自体の入力欄やインスペクターモーダルを、ワークスペースのフォーカスとして誤採用しない。
- [ ] メインエディタ、左右サイドバー内の入力欄、非フォーカス要素を含む回帰テストがある。

## UX-002: 検査用クリックが元のUI操作も発火させる

**優先度:** P0

### 課題

検査用のクリックを捕捉しても、イベントの既定動作や後続ハンドラーを停止していない。そのため「何があるか調べるためのクリック」が、タブ移動、チェックボックスの変更、リンク遷移、ツールバーボタンの実行などを同時に発生させる。

観察行為が観察対象を変更するため、インスペクターとして安全ではない。

### 根拠

- `src/main.ts:67-82` の `handleClick` は結果モーダルを開くだけで、`preventDefault()`、`stopPropagation()`、`stopImmediatePropagation()` のいずれも呼んでいない。
- `src/main.ts:89` でキャプチャフェーズからモーダルを開いても、すでに確定したイベントターゲットへの後続イベント処理は自動では止まらない。
- Obsidianのワークスペースペインには、ノート内チェックボックス、タブ、リンク、検索結果、ビュー固有の操作ボタンが日常的に存在する。
- Nielsen Norman Groupの「Error Prevention」に照らすと、確認より先に、誤操作が起きる条件自体を除去すべきである。

### 通常利用での影響

ユーザーは検査したいUIを正確にクリックするほど、そのUI本来の操作も実行してしまう。特にチェックボックス、リンク、ファイル一覧、コミュニティプラグインのボタンを検査しにくく、結果画面を閉じた後に状態を戻す作業が発生する。

### 改善方針

UX-001の「現在のコンテキストを検査」を主導線にすれば、検査専用クリック自体を多くのケースで不要にできる。ポインターピッカーを残す場合は、対象選択中の1クリックを消費し、元UIのアクションを実行しない設計にする。

### 受け入れ条件

- [ ] 検査対象を選ぶクリックで、リンク遷移、ボタン実行、チェック変更、タブ切り替えが発生しない。
- [ ] 検査モードでない通常時のクリック挙動には影響しない。
- [ ] ボタン、リンク、チェックボックス、タブを使ったイベント伝播の回帰テストがある。
- [ ] 対象選択のために元UIを実行する必要がある場合は、その理由と結果が事前に明示される。

## UX-003: 検査待機状態が不可視かつ解除不能である

**優先度:** P0

### 課題

コマンド実行後、プラグインは次のクリックを待ち続けるが、表示は一時的なNoticeだけである。Noticeが消えた後も待機状態は残り、`Escape`、再実行、画面上のキャンセル操作、タイムアウトのいずれもない。

### 根拠

- `src/main.ts:62-64` は `inspectionArmed = true` にしてNoticeを表示する。
- `src/main.ts:67-72` では次のクリックが発生するまで待機状態を維持する。
- 待機状態を解除する操作は、次のクリックかプラグインのアンロードしか実装されていない。
- Nielsen Norman Groupの「Visibility of System Status」と「User Control and Freedom」では、継続中の状態を見せ、明確な退出手段を提供することが基本原則とされる。

### 通常利用での影響

コマンド実行後に通知や別作業へ注意が移ると、数分後の無関係なクリックで突然モーダルが開く。ユーザーは原因を思い出す必要があり、プラグインがランダムに割り込んだように感じる。

### 改善方針

対象選択モードを残す場合は、待機中であることを継続表示する。`Escape` と明示的なキャンセル操作を提供し、コマンド再実行は待機の解除として扱う。長時間放置へのタイムアウトも設定する。

UI Pro MaxのUXデータセットでは、この「継続モードの可視化と解除」に直接一致する項目を確認できなかったため、ここでは上記の一般的なインタラクション原則と、実装上の無期限状態を根拠としている。

### 受け入れ条件

- [ ] 待機中は、Noticeが消えた後も状態と次の操作が視認できる。
- [ ] `Escape` で待機状態を解除できる。
- [ ] コマンドの再実行で待機状態を解除できる。
- [ ] 解除時に「検査をキャンセルした」ことが短く通知される。
- [ ] 一定時間操作がない場合に待機状態が自動解除される。
- [ ] 待機中、キャンセル済み、対象取得済みの各状態がテストされる。

## UX-004: 検査対象外のクリックを有効な結果として扱う

**優先度:** P1

### 課題

案内は「workspace paneをクリック」としているが、実装はドキュメント全体の任意のクリックを受け付ける。リボン、タイトルバー、ステータスバーなどLeaf外をクリックした場合でも、フォーカス中またはアクティブな別Leafへフォールバックし、クリック位置とは別の結果を表示する可能性がある。Leafを解決できなければ `area: unknown` が生成される。

### 根拠

- `src/main.ts:84-90` はドキュメント全体にクリックリスナーを登録している。
- `src/main.ts:73-81` はクリック要素が存在すれば、ワークスペースペイン内かを検証せず結果を開く。
- `src/context.ts:84` はクリック位置にLeafがなくても、フォーカス中またはアクティブなLeafへフォールバックする。
- `src/context.ts:120-131` は `unknown` であっても `area` を必ず条件へ出力する。

### 通常利用での影響

ユーザーは案内どおりにクリックできなかったことに気づかず、別ペインの条件や `area: unknown` を有効な設定候補として受け取る。誤りがエラーではなく正常結果に見えるため、設定後に初めて問題が判明する。

### 改善方針

対象選択中は選択可能なLeafをホバー表示し、Leaf外を有効な選択として確定しない。対象外をクリックした場合は、理由と次の操作を短く示して選択モードを継続する。`unknown` は診断値として表示しても、推奨条件やコピー対象には含めない。

### 受け入れ条件

- [ ] 選択可能なペインがホバーまたはアウトラインで識別できる。
- [ ] Leaf外のクリックを、別のアクティブLeafへ黙って置き換えない。
- [ ] 対象外をクリックした場合、何を選択すべきかが表示される。
- [ ] `area: unknown` を推奨条件としてコピーまたは確定できない。
- [ ] リボン、タイトルバー、ステータスバー、モーダル上のクリックがテストされる。

## UX-005: 「候補」と「完成した条件」の区別がなく、過剰に限定された条件を誘発する

**優先度:** P1

### 課題

画面見出しは `Stable condition candidates` だが、その直下には `area`、`viewType`、`mode` をすべてAND結合した1つのYAMLが表示される。各値が選択可能な候補なのか、推奨される完成条件なのかが不明である。

また、`Stable` はDOMクラスとの相対比較では妥当でも、永続性を保証する表現としては強すぎる。`area` はユーザーがペインを移動すると変わり、コミュニティプラグインの `viewType` はプラグイン実装に依存し、`mode` はMarkdownViewにしか存在しない。

### 根拠

- `src/context-modal.ts:21-29` は候補の表と、全候補を結合したコードブロックを同列に表示する。
- `src/context.ts:120-131` は存在する値をすべて条件へ追加し、スコープの粒度を選ぶ余地がない。
- 例えば「すべてのMarkdownエディタ」を意図するユーザーに `area: main` まで含めると、ポップアウトや別領域へ移動した同じビューでは動作しなくなる。
- IDEの `when` 条件に慣れたユーザーでも、Obsidian固有IDの安定性や条件生成側の推奨意図までは推測できない。

### 通常利用での影響

ユーザーは表示されたYAMLを完成形だと解釈し、必要以上に限定されたルールを作りやすい。設定直後は動いても、ペイン移動、分割、ソース/プレビュー切り替えで動かなくなり、「ホットキーが不安定」と判断される。

### 改善方針

「検出した値」「推奨スコープ」「生成された最終条件」を分離する。エリア、ビュー種別、Markdownモードを含めるか選べるようにし、プリセットとしてスコープ粒度を提示する。最終条件の上には、人間が読める一致範囲を表示する。

推奨例:

```text
この条件は、左右の配置に関係なく Agent Chat ビューで一致します。
```

### 受け入れ条件

- [ ] 各フィールドがAND条件として結合されることが明示される。
- [ ] `area`、`viewType`、`mode` を含めるかユーザーが選択できる、または妥当な粒度のプリセットを選べる。
- [ ] 生成条件が「どこに一致し、どこには一致しないか」を自然言語で確認できる。
- [ ] 推奨する既定値と、その理由が表示される。
- [ ] `Stable` は「DOMクラスより安定した候補」など、保証範囲が分かる表現へ変更される。
- [ ] ペイン移動、ポップアウト、Markdownモード切り替え時の一致範囲をテストできる。

## UX-006: 結果画面に次の行動がなく、設定作業が完結しない

**優先度:** P1

### 課題

モーダルは読み取り専用の表とコードを表示するだけで、条件のコピー、再検査、キー割り当て作成への遷移がない。ユーザーはコードをドラッグ選択してコピーし、モーダルを閉じ、別の場所で作業を再開する必要がある。

### 根拠

- `src/context-modal.ts:17-47` は情報を生成するだけで、ボタンやフッターアクションを作成していない。
- `styles.css:40-43` の `user-select: text` が唯一の持ち出し手段であり、コピー対象の正確さやコピー成功を保証しない。
- 機能説明では「新しいキー割り当て機能のヒントとなる元情報」を提供することが目的であり、結果を次工程へ渡せなければタスクが途中で止まる。
- UI Pro Maxの検証済みガイダンス「Confirmation Messages」では、コピーのような完了操作に短い成功フィードバックを返すことが推奨される。

### 通常利用での影響

複数箇所を試すたびに、コマンド実行、クリック、手動選択、コピー、閉じる、再実行を繰り返す。IDEでのキーバインド設定に慣れたユーザーほど、この手数と明確な完了状態の欠如を非効率に感じる。

### 改善方針

現フェーズでも `条件をコピー` と `別の場所を検査` を提供する。キー割り当て機能が実装された時点で、主CTAを `この条件でキー割り当てを作成` に置き換える。コピー後はボタンラベルまたはNoticeで成功を即時に知らせる。

### 受け入れ条件

- [ ] 生成条件を1操作で正確にコピーできる。
- [ ] コピー成功が視覚的に通知される。
- [ ] モーダルから別の場所の検査を再開できる。
- [ ] 主要アクションをTabで移動し、EnterまたはSpaceで実行できる。
- [ ] キー割り当て機能の実装後は、選択した条件を再入力せず作成画面へ渡せる。

## UX-007: 主目的より内部診断情報が強く、判断に必要な情報階層になっていない

**優先度:** P1

### 課題

画面の大半を `View details` と `DOM and focus diagnostics` が占め、長いクラス名や真偽値が常時展開されている。一方、ユーザーが最初に判断すべき「どの範囲でホットキーが有効になるか」「この条件をどう使うか」は弱い。

DOM診断はトラブルシューティングには有用だが、キー割り当ての主タスクには二次情報である。Webエンジニアというペルソナであっても、内部情報を理解できることと、毎回すべてを読む必要があることは別である。

### 根拠

- 添付画面では、DOM診断の2テーブルがモーダル下半分以上を占めている。
- `src/context-modal.ts:31-46` は `View details` とDOM診断を常時展開する。
- `styles.css:45-59` は横幅に応じて診断表を2列から1列へ変えるだけで、情報量や縦方向の優先順位は変えない。
- `Leaf source`、`Tag`、`Role`、`Classes` は実装診断用語であり、ホットキーの一致範囲を直接説明しない。
- Nielsen Norman Groupの「Aesthetic and Minimalist Design」では、主目的と関係の薄い情報を常時提示すると、必要な情報の相対的な可視性が下がるとされる。

### 通常利用での影響

一般的なノートPCの画面高ではモーダル内スクロールが必要になり、条件と操作を見ながら診断情報を確認しにくい。長いクラス名は折り返され、クリック対象とフォーカス対象の差も表を読み比べないと分からない。

### 改善方針

初期表示を次の順番にする。

1. 人間が読める対象サマリー
2. 推奨スコープと一致範囲
3. 生成条件
4. コピー、再検査、割り当て作成のアクション
5. 折りたたまれた `Advanced diagnostics`

`Clicked element` と `Focused element` が異なる場合だけ差分を要約し、詳細なタグ、Role、Classes、`Leaf source` は展開後に表示する。

UI Pro MaxのUXデータセットでは「診断情報の段階的開示」に直接一致する項目を確認できなかったため、ここでは主タスクとの関連度、実画面で占める面積、上記のミニマリズム原則を根拠としている。

### 受け入れ条件

- [ ] 画面高768pxで、対象サマリー、推奨条件、主要アクションがスクロールせず確認できる。
- [ ] DOMとフォーカスの詳細診断は既定で折りたたまれている。
- [ ] クリック対象とフォーカス対象が異なる場合、表を比較せず差を認識できる。
- [ ] 長い `viewType` やクラス名を展開しても、モーダルの横スクロールやレイアウト崩れが起きない。
- [ ] 診断情報を折りたたんでも、キー割り当て判断に必要な情報が失われない。

## 維持すべき点

- Obsidian標準の `Modal` と `Notice` を使用しており、独自ウィンドウより操作の一貫性が高い。
- 色、罫線、余白にObsidianのCSS変数を使用しており、テーマ追従の方向性は正しい。
- DOM値を安定条件から分離し、「Obsidianまたはプラグインのバージョンで変わり得る」と警告している点は維持すべきである。
- 700px以下で診断表を1列にする対応は、詳細表示のフォールバックとして維持できる。

## 今回課題化しない項目

ユーザー指定の「通常ケースにないNon-essentialな課題を除外する」という条件に基づき、次は課題化しない。

- モバイル固有のタッチターゲット、セーフエリア、端末回転: 対象ペルソナの主要利用環境ではなく、今回の判断に必要な証拠もない。
- 独自ブランディングや装飾的なビジュアル刷新: Obsidian内ツールではネイティブな見た目を維持する方が適切である。
- 完全な多言語化: 現時点では機能の正しさと操作完結性より優先度が低い。
- 検査履歴、複数結果の比較、条件テンプレート管理: 将来の効率化候補だが、単一のキー割り当てを作る基本フローの成立には必須ではない。
- 稀なShadow DOM構造ごとの個別UI: 現実装は診断処理で考慮しており、添付画面から通常利用上の問題は確認できない。

## 参照基準

- [Nielsen Norman Group: 10 Usability Heuristics for User Interface Design](https://www.nngroup.com/articles/ten-usability-heuristics/)
- [W3C: Understanding Success Criterion 2.1.1 Keyboard](https://www.w3.org/WAI/WCAG22/Understanding/keyboard)
- UI Pro Max UX dataset: `Keyboard Navigation`、`Confirmation Messages`
- リポジトリ内の実装とObsidian実機表示
